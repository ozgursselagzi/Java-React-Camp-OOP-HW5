package com.company;

public interface ıNeroLogManager {
    void addToDataBase(NeroCustomer neroCustomer);
    void removeFromDataBase(NeroCustomer neroCustomer);
    void updateInDataBase(NeroCustomer neroCustomer);
}
